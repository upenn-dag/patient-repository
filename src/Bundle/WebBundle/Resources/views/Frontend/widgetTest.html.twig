{# Changes the theming for rendering widgets... #}

{# widget_theme widget "AccardWebBundle::widgets.html.twig" #}

{#}
{% for row in grid.rows %}
	{% for column in row.columns %}
		{{ widget(column.widget) }}
	{% endfor %}
{% endfor %}
#}

{{ canvas('1x4:2x2', 'test/route', widget) }}

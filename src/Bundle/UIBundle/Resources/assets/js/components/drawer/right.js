'use strict';

var React = require('react');

var RightDrawer = React.createClass({
  render: function() {
    return (
      <h2>This is the right drawer</h2>
    );
  }
});

module.exports = RightDrawer;
